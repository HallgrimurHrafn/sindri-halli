extends layout

block content
  h1= title
  br

  div.newThread
    a(href='/newThread')
      button(type='submit' name='writeThread') New Thread
  br

  hr
  table
    thead
      tr
        th User
        th Title
        th Date
    tbody
      each thread in threads
        -id = '/threadid=' + thread.id
        tr
          td= thread.name
          td
            a(href=id)= thread.title
          td= thread.date
